<nav class="navbar navbar-expand-lg navbar-light bg-dark">

  <%= link_to 'the blog', root_path, class: "navbar-brand text-light text-uppercase", id:"logo" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to 'Bloggers', users_path, class: "nav-link text-light"%>
      </li>
      
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Articles
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <%= link_to 'See all Articles', articles_path, class:"dropdown-item" %>
          <% if logged_in?%>
           <%= link_to 'Write an Article', new_article_path, class:"dropdown-item" %>
          <%end%>
        </div>
      </li>
      <% if logged_in?%>
      <li class="nav-item active nav-link text-light">
        <%= "(Admin)" if current_user.admin?%> 
      </li>
      <li class="nav-item active">
           <%= link_to current_user.username, user_path(current_user), class: "nav-link text-light"%>
        </li>
        <li class="nav-item active">
          <%= link_to 'Logout', logout_path, class: "nav-link text-light", method: :delete%>
        </li>
      <% else %>
       <li class="nav-item active">
          <%= link_to 'Sign Up', signup_path, class: "nav-link text-light"%>
        </li>
        <li class="nav-item active">
        <%= link_to 'Login', login_path, class: "nav-link text-light"%>
      </li>
      <% end %>
    </ul>
  </div>
</nav>
