<div class="container col-12">
  <div class="jumbotron main-jumbo text-center text-light">
    <h1 class="display-1 ">Welcome to the Blog!</h1>
    <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
    <hr class="my-4">
    <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
    <p class="lead">
    <% if logged_in?%>
      <%= link_to 'View Your Profile', user_path(current_user), class:"btn btn-outline-light btn-lg" %>
    <% else %>
      <%= link_to 'Sign Up', signup_path, class:"btn btn-outline-light btn-lg" %>
    <% end %>
    
     
    </p>
  </div>
  
<div class="apple_pagination">
    <%= will_paginate @articles, :container => false %>
</div>

<%= render 'article'%>