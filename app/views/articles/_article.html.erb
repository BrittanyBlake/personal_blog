
    <div class="card-columns" id="articles">
        <% @articles.each do |article|%>
            <div class="card text-center p-3">
                <div class="card-body">
                    <h5 class="card-title"> <%= link_to article.title, article_path(article), class:"text-muted" %></h5>
                    <p class="card-text"> <%= truncate(article.body, length:50)%> </p>
                    <%= link_to 'Read this article', article_path(article), class:"btn btn-outline-info" %>
                    <%= link_to 'Edit this article', edit_article_path(article), class:"btn btn-outline-success" %>
                    <%= link_to 'Delete this article', article_path(article), method: :delete, data: {confirm: "Are you sure?"} , class:"btn btn-outline-danger" %>
                    <p class="card-text">
                        <small class="text-muted"> Created <%=time_ago_in_words(article.created_at)%> ago & updated <%=time_ago_in_words(article.created_at)%> ago</small>
                    </p>
                </div>
            </div>
        <% end %>
    </div>